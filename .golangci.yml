version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gocritic
    - misspell

formatters:
  enable:
    - gofmt

issues:
  exclude-rules:
    - linters: [gosec]
      text: "G402|G107"
    - path: _test\.go
      linters: [errcheck, gocritic]
